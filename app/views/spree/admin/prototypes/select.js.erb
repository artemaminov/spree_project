<% @prototype_properties.sort_by{ |prop| -prop[:id] }.each do |prop| %>
  $("a.spree_add_fields").click();
  $(".product_property.fields:first select").val("<%= prop.name %>").trigger('change');
<% end %>
