<% max_level = 1 %>
<% cache [I18n.locale, @taxonomies, max_level] do %>
  <% @taxonomies.each do |taxonomy| %>
    <% taxon = taxonomy.taxons.first %>
    <%= link_to taxon.name, taxon.permalink, class: 'item' %>
  <% end %>
<% end %>